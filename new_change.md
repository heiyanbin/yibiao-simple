# Add Multi-tenant feature


## Model Setting
- API key and base url is a backend system setting, and should not be exposed to the user
- Only show a dropbox on the sidebar, allow user to select different models configured in the config file.

## User Management

### User Register and Loggin Feature

### User-specific Data
- his own set of prompts, e.g. for the input document analysis step, user could have multiple prompts to choose in different jobs
- his jobs. 
  - 一个作业就是对系统的一次使用，包括上传的招标文件，每个步骤使用的实际提示词，中间产物，时间等




